// let firstName = "Deeqa", "Hannan"; //normal variables can only hold one value
let firstNames = ["Deeqa", "Hannan"]; //data structures

console.log(firstNames);
firstNames.push("Mahmoud"); //add to the array
console.log(firstNames); //need to reprint the conosole.log

console.log(firstNames.length);

//arrays allow you to add duplicates. Sets are unique and will not allow you to add duplicates
